# Operações com Vetores
# - Dados os vetores `a <- c(1, 2, 3)` e `b <- c(4, 5, 6)`, calcule a soma, subtração, multiplicação e divisão elemento a elemento.

a <- c(1,2,3)
b <- c(4,5,6)

soma <- a + b
sub <- a - b
mult <- a * b
div <- a / b

soma
sub
mult
div
