#!/usr/bin/env bash

# Escreva um script que execute um comando (por exemplo, ls) e verifique se ele foi executado com sucesso ou não.

test ls && echo 'Comando ls executado' || echo 'comando ls não executado'
